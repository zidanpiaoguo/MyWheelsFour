<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzy.mywheelsfour">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:name=".App.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ToolbarTest.TestAcitvity" />
        <activity android:name=".Test.ProgressDialog.proDialogTest" />
        <activity android:name=".Test.switchButton.switchButton" />
        <activity android:name=".Test.ImageView.ImageAcitivity">

        </activity>

        <activity android:name=".Test.recycleview.RecycleViewActivity">



        </activity>
        <activity android:name=".Test.Coordinator.CoordinatorActivity">

        </activity>

        <activity android:name=".Test.Splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Test.Splash.GuideActivity">

        </activity>
    </application>

</manifest>